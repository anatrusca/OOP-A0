import numpy as np
# Create a list containing all numbers from 1 to 10
numbers = list(range(1, 11))
# Create a numpy array from this list
array = np.array(numbers)
# Print the numpy array
print(array)
